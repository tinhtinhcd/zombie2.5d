extends Resource
class_name LevelDifficultyData

# Keep this resource focused on tunable progression values.
# Add more parameters here later instead of spreading them across gameplay scripts.

@export var health_multiplier: float = 1.0
@export var spawn_multiplier: float = 1.0
@export var pressure_rating: float = 1.0
